services:
  web:
    image: nginx:1.27
    ports:
      - "8080:80"
  db:
    image: postgres:16
    volumes:
      - dbdata:/var/lib/postgresql/data
      - /var/lib/scratch

volumes:
  dbdata:

networks:
  backend:
